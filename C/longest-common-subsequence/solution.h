#ifndef SOLUTION_H
#define SOLUTION_H

#include <stdio.h>
#include <string.h>
#include <stdlib.h>

#define MAX(a, b) (((a) > (b)) ? (a) : (b))

// Function prototype
int longestCommonSubsequence(char *text1, char *text2);

#endif // SOLUTION_H
