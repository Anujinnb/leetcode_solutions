#ifndef SOLUTION_H
#define SOLUTION_H

#include <stddef.h>
#include <stdbool.h>

void processString(const char *s, char *processed, size_t size);
bool isPalindrome(const char *s);

#endif // SOLUTION_H
